<aside class="span4">
  <section>
    <h1>
      Account Information
    </h1>
  </section>
  <section>

    Name : <%= @user.name %>
    Email : <%= @user.email %>

    <ul class="user-roles">
      User Role(s):
      <% @user.roles.each do |ur| %>
          <li>
            <%=   ur.role_name %>
          </li>

      <% end %>
    </ul>

  </section>

  <%= link_to "edit",  edit_admin_user_path(@user) %>

  <%= @user.inspect %>

</aside>
